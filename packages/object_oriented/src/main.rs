mod collect;
mod post;
use collect::AveragedCollection;
use object_oriented::{Button, Screen, SelectBox};

fn main() {
    // One
    // let mut c = AveragedCollection::new();
    // c.add(3);
    // c.add(4);
    // c.add(5);
    // println!("{}", c.average());

    // Second
    // let screen = Screen {
    //     components: vec![
    //         Box::new(SelectBox {
    //             width: 75,
    //             height: 10,
    //             options: vec![
    //                 String::from("Yes"),
    //                 String::from("Maybe"),
    //                 String::from("No"),
    //             ],
    //         }),
    //         Box::new(Button {
    //             width: 50,
    //             height: 10,
    //             label: String::from("OK"),
    //         }),
    //     ],
    // };
    // screen.run();
   
}
